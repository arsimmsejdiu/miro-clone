import { BoardIdPageProps } from "@/interfaces/board-id-interface";

const BoardIdPage = ({
    params,
  }: BoardIdPageProps) => {
    return (
        <div>
            Board details page
        </div>
    )
  }

  export default BoardIdPage